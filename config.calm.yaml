# Profile overlay (calm / low-frequency polling)
# Copy these sections into `config.yaml` when switching mode.

trading:
  poll_interval_sec: 60
  dashboard_refresh_sec: 60

closing:
  tp_sync_interval_sec: 120.0
  tp_max_updates_per_cycle: 10
